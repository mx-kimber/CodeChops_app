<%= form_with(model: [@user, @chop]) do |form| %>
  <% if @chop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chop.errors.count, "error") %> prohibited this chop from being saved:</h2>

      <ul>
      <% @chop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :problem %>
    <%= form.text_area :problem, rows: 4 %>
  </div>

  <div class="field">
    <%= form.label :solution %>
    <%= form.text_field :solution %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.collection_select :category_id, Category.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit "Create Chop" %>
  </div>
<% end %>
